spring:
  application:
    name: auth-service
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: ${APP_JWT_SECRET:01234567890123456789012345678901}
    expiration: ${APP_JWT_EXPIRATION:3600000}
